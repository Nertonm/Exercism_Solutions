
struct aluno{
    int id;
};

typedef struct no No;
typedef struct aluno Aluno;

No* criar();
int vazia(No*);
int rmNo(No*);
int preOrdem(No*);
int posOrdem(No*);
int emOrdem(No*);
int Altura(No*);
int contNo(No*);
int inserirNo(No*, Aluno);
 
